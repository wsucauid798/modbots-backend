[project]
name = "modbots-ml"
version = "0.0.1-beta"
description = "Mod Bots ML service: model inference for bot minds"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "httpx>=0.28",
    "uvicorn[standard]>=0.32",
]
